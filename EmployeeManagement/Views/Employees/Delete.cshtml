@model int?

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    @using (Html.BeginForm("Delete", "Employees", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        string searchValue = Convert.ToString(ViewBag.SearchValue);
        string sortField = Convert.ToString(ViewBag.SortField);
        string pageNumber = Convert.ToString(ViewBag.PageNumber);

        @Html.Hidden("searchValue", searchValue)
        @Html.Hidden("sortField", sortField)
        @Html.Hidden("pageNumber", pageNumber)
        @Html.Hidden("id", Model)

        <input type="submit" value="Delete" /> 
            @Html.ActionLink("Back to List", "Index")
    }
</div>
